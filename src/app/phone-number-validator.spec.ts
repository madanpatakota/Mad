import { PhoneNumberValidator } from './phone-number-validator';

describe('PhoneNumberValidator', () => {
  it('should create an instance', () => {
    expect(new PhoneNumberValidator()).toBeTruthy();
  });
});

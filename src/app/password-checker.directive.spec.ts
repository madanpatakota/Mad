import { PasswordCheckerDirective } from './password-checker.directive';

describe('PasswordCheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordCheckerDirective();
    expect(directive).toBeTruthy();
  });
});
